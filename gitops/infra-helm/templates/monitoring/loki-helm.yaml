# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/refs/heads/main/argoproj.io/application_v1alpha1.json
{{- if .Values.monitoring.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.cluster.name }}-loki
  labels:
    cluster: {{ .Values.cluster.name | quote }}
    app: loki
  namespace: {{ .Values.cluster.namespace | quote }}
  annotations:
    argocd.argoproj.io/sync-wave: "1"
    app.kubernetes.io/part-of: monitoring
    {{- if .Values.notifications }}
    {{ toYaml .Values.notifications | indent 4 }}
    {{- end }}
spec:
  project: {{ .Values.cluster.name }}
  destination:
    server: {{ .Values.cluster.url | default "https://kubernetes.default.svc" | quote }}
    namespace: monitoring
  syncPolicy:
    {{- if .Values.sync }}
    automated:
      prune: true
      selfHeal: true
    {{- end }}
    syncOptions:
      - CreateNamespace=true
    managedNamespaceMetadata:
      labels:
        pod-security.kubernetes.io/enforce: privileged
        pod-security.kubernetes.io/warn: privileged
        pod-security.kubernetes.io/audit: privileged
  source:
    repoURL: https://grafana.github.io/helm-charts
    chart: loki
    targetRevision: {{ .Values.monitoring.loki.version | quote }}
    helm:
      releaseName: loki
      values: |
        # chart-level keys
        deploymentMode: {{ .Values.monitoring.loki.deploymentMode | default "SingleBinary" | quote }}
        {{- if .Values.monitoring.loki.singleBinary }}
        singleBinary:
{{ toYaml .Values.monitoring.loki.singleBinary | indent 10 }}
        {{- end }}

        simpleScalable:
          replicas: 0
          backend:
            replicas: 0
          read:
            replicas: 0
          write:
            replicas: 0

        backend:
          replicas: 0
        read:
          replicas: 0
        write:
          replicas: 0

        # main loki config block
        loki:
{{ toYaml (omit .Values.monitoring.loki "version" "gateway" "singleBinary" "deploymentMode") | indent 10 }}

        # gateway is a sibling, not inside 'loki:'
        gateway:
{{ toYaml .Values.monitoring.loki.gateway | indent 10 }}
{{- end }}

