{{- if and .Values.security.enabled .Values.security.keycloak.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ include "infra.appName" (dict "root" . "name" "keycloak") }}
  labels:
    cluster: {{ .Values.cluster.name | quote }}
    app: keycloak
  namespace: {{ .Values.cluster.namespace | quote }}
  annotations:
    # Stage 1: optional stateful identity service after storage foundations.
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: {{ include "infra.projectName" . }}
  destination:
    server: {{ .Values.cluster.url | default "https://kubernetes.default.svc" | quote }}
    namespace: security
  syncPolicy:
    {{- if .Values.sync }}
    automated:
      prune: true
      selfHeal: true
    {{- end }}
    syncOptions:
      - FailOnSharedResource=true
      - CreateNamespace=true
      - SkipDryRunOnMissingResource=true
  source:
    repoURL: https://charts.bitnami.com/bitnami
    chart: keycloak
    targetRevision: {{ .Values.security.keycloak.version | quote }}
    helm:
      values: |
        production: true
        proxy: edge
        httpRelativePath: "/"
        replicaCount: 2

        auth:
          adminUser: admin
          existingSecret: keycloak-admin-secret
          passwordSecretKey: admin-password

        persistence:
          enabled: true
          size: 10Gi
          storageClass: longhorn

        postgresql:
          enabled: true
          primary:
            persistence:
              enabled: true
              size: 10Gi
              storageClass: longhorn

        ingress:
          enabled: true
          ingressClassName: nginx
          hostname: {{ printf "keycloak.%s.nl" .Values.cluster.name }}
          tls: true
          annotations:
            nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
            cert-manager.io/cluster-issuer: "letsencrypt-dns"
{{- end }}
