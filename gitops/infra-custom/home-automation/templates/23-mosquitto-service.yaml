apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.mqtt.service.name }}
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app: mosquitto
  ports:
    - name: mqtt
      protocol: TCP
      port: {{ .Values.mqtt.service.port }}
      targetPort: mqtt
