apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: home-assistant
configMapGenerator:
  - name: home-assistant-rainbird-package
    files:
      - rainbird.yaml=packages/rainbird.yaml
  - name: home-assistant-rainbird-dashboard
    files:
      - rainbird.yaml=rainbird-dashboard.yaml
      - lovelace-rainbird-card.yaml=lovelace-rainbird-card.yaml
generatorOptions:
  disableNameSuffixHash: true
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
  labels:
    app.kubernetes.io/name: home-assistant
    app.kubernetes.io/component: rainbird-package
